# SMS Spam Detector Frontend

This frontend is built as a lightweight static UI for the SMS classifier backend.

## Run locally

1. Start the backend from the project root:

```powershell
python backend/run.py
```

2. Open the app in your browser at:

```text
http://localhost:5000/
```

The frontend is served by the Flask backend, so no separate static server is required.
